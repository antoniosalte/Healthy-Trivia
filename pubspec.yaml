name: healthytrivia
description: Healthy Trivia

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

  provider: ^4.1.2
  firebase_auth: ^0.16.1
  cloud_firestore: ^0.13.6
  flutter_webview_plugin: ^0.3.11
  flutter_svg: ^0.17.4
  screenshot: ^0.2.0
  esys_flutter_share: ^1.0.2
  shared_preferences: ^0.5.7+3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.svg
